I’m reviewing the repository instructions and the affected daemon paths first, then I’ll patch the three requested fixes and run the relevant tests plus a build.I’ve confirmed the bridge already has partial attestation hardening, but I still need to verify the stale-cache behavior, how stdio singleton children are managed, and what tests cover those paths before editing.The current tree already contains singleton-lease machinery. I’m checking whether it actually covers the stale stdio child case end to end or only handles one narrow lease path, then I’ll patch tests and source together.I need to separate pre-existing edits from the work I’m about to do, so I’m checking the git state before patching anything.Some of the requested changes are already present in the worktree, but I still need to verify they’re complete and consistent with the tool schema and boot path before I decide whether to keep or refine them.I’m ignoring the empty critique, but I am checking the prior failed artifact path once more in case it contains the missing revision note that didn’t make it into the prompt body.I’ve got enough context. The remaining work is verification-driven now: run the changed tests, inspect any failures, then only patch what is still incomplete before rebuilding `daemon/dist`.The build is clean. The first test command was my mistake against Vitest’s CLI, so I’m rerunning the targeted suite with the correct invocation and then the full suite.The code is built, but the test runner is blocked by the sandbox’s process-spawn restriction. I’m trying a single-threaded Vitest mode next so I can still exercise the suite inside this environment.The code changes themselves are compiling. I’m collecting the final diff summary now and checking whether `daemon/dist` produced any tracked deltas before I close this out.Implemented the requested hardening in the daemon layer.

The changes are in [eights-bridge.ts](C:/AiAppDeployments/AgentSmith/daemon/src/bridges/eights-bridge.ts), [mcp-client.ts](C:/AiAppDeployments/AgentSmith/daemon/src/bridges/mcp-client.ts), [invariants.ts](C:/AiAppDeployments/AgentSmith/daemon/src/inspector/invariants.ts), and [tools.ts](C:/AiAppDeployments/AgentSmith/daemon/src/mcp/tools.ts). I also added coverage in [governance.test.ts](C:/AiAppDeployments/AgentSmith/daemon/tests/governance.test.ts), [invariants-cache.test.ts](C:/AiAppDeployments/AgentSmith/daemon/tests/invariants-cache.test.ts), and [mcp-client.test.ts](C:/AiAppDeployments/AgentSmith/daemon/tests/mcp-client.test.ts).

Behaviorally:
- N8 self-attestation now refuses any non-`agentsmith` consumer on the local-hash comparison path, and the public MCP schema only accepts `consumer: "agentsmith"`.
- Constitution caching is now keyed by resolved path and refreshes automatically when file `mtime` or `size` changes.
- The Eights stdio bridge now uses a singleton lease/orphan-reap guard so stale child daemons from prior sessions are reaped before reconnect.

Checks:
- `npm run build` in `daemon/`: passed.
- `npm test` and targeted `vitest` runs: blocked by this sandbox with `spawn EPERM` before test discovery, so I could not complete the suite here.
- `daemon/dist` was rebuilt successfully; there was no remaining tracked `dist` diff after the build.