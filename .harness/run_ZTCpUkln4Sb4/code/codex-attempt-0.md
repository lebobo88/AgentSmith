I’m starting by reading the repo’s local instructions and the affected modules so I can patch the three failure modes without widening scope. After that I’ll inspect the existing tests and rebuild path before editing anything.I have the local contract. Next I’m tracing the attestation path, the constitution cache, and the daemon startup/runtime hooks that could leave stale stdio children behind, plus the current test coverage around those paths.There is already some prior N8-related work in the tree, including a reattest tool path, so I’m checking current source state before touching it. I’m also looking for the child-process lifecycle code, because the orphan-reap fix needs to land in the actual stdio bridge layer rather than as a sidecar workaround.I’ve confirmed the tree already contains unrelated prior work for live N8 reattestation, but the three requested fixes are still the right scope. I’m drilling into the existing bridge/client lifecycle now so I can patch the consumer pinning, stale constitution cache, and child-process reaping without regressing the current retry/refusal flow.I’ve isolated the likely write points. The constitution cache fix is straightforward. The child-process guard needs a little more care because it has to prevent pid reuse mistakes while still reaping stale TheEights stdio children before they pile up on `~/.eights/state.db`. I’m checking the SDK transport shape so I can attach the guard at the right layer and test it cleanly.Plan:
1. Patch `eights-bridge` so self-attestation cannot compare Smith’s local hash against another consumer’s constitution, and update the exposed tool contract to match.
2. Replace the singleton constitution cache with a per-path cache that refreshes on stat changes.
3. Add a single-instance orphan-reap lease in `mcp-client` for the spawned TheEights stdio child, then add focused tests for the new guard paths and the attestation/cache behavior.
4. Rebuild `daemon/dist` and run the full daemon test suite.I have enough context to edit. I’m patching the bridge/tool contract first, then the cache, then the singleton lease/reap guard in the stdio client, followed by targeted tests for each behavior.I hit one test-setup issue while adding coverage: the temp constitution directory needs to be recreated per test, not shared across the whole file. I’m correcting that before running the suite.The source edits are in. I’m running the daemon build now to catch type or module issues before I trust the new tests.The TypeScript build is clean. I’m running the full Vitest suite now; if anything fails, I’ll tighten the patches instead of papering over it.The suite didn’t fail on the code; Vitest itself is trying to `fork` workers and the sandbox is rejecting that with `spawn EPERM`. I’m rerunning the full suite in a single-threaded pool so I can still verify the actual tests end to end in this environment.The default Vitest CLI is blocked here by Vite’s Windows `net use` probe, not by the code under test. I’m trying one last workaround: launching Vitest programmatically with a patched `child_process.exec` so the full suite can still run inside this sandbox without changing repo config.Vitest is still blocked deeper in the toolchain by `esbuild` process spawning, so a true suite run is not possible in this sandbox. I’m switching to deterministic smoke checks against the compiled JS for the new paths, then I’ll give you the exact blocker alongside the clean build result.The source and generated `dist` both build, and the new behavior smoke checks are passing against the compiled JS. I’m doing one final pass over the changed file set and result status so the closeout is exact.Implemented the three requested fixes.

Files changed:
- [daemon/src/bridges/eights-bridge.ts](C:/AiAppDeployments/AgentSmith/daemon/src/bridges/eights-bridge.ts:645): `constitutionAttest()` now fail-closes on any non-`agentsmith` consumer instead of comparing Smith’s local hash against a peer constitution, and the Eights bridge now uses a singleton lease key for child-process reaping ([line 202](C:/AiAppDeployments/AgentSmith/daemon/src/bridges/eights-bridge.ts:202)).
- [daemon/src/mcp/tools.ts](C:/AiAppDeployments/AgentSmith/daemon/src/mcp/tools.ts:126): narrowed `agentsmith.constitution.attest` to Smith’s own consumer so the public tool contract matches the bridge behavior.
- [daemon/src/inspector/invariants.ts](C:/AiAppDeployments/AgentSmith/daemon/src/inspector/invariants.ts:73): replaced the old module-global snapshot cache with a per-path cache keyed by resolved path and invalidated on `mtimeMs` or file size change.
- [daemon/src/bridges/mcp-client.ts](C:/AiAppDeployments/AgentSmith/daemon/src/bridges/mcp-client.ts:129): added singleton lease claim/reap/release helpers and wired them into connect/close so stale stdio children from prior sessions are reaped before a new Eights child is started.
- [daemon/tests/governance.test.ts](C:/AiAppDeployments/AgentSmith/daemon/tests/governance.test.ts:402): added coverage for non-`agentsmith` attestation refusal and tool-schema restriction.
- [daemon/tests/invariants-cache.test.ts](C:/AiAppDeployments/AgentSmith/daemon/tests/invariants-cache.test.ts:26): added coverage for path-keyed caching and auto-refresh on file change.
- [daemon/tests/mcp-client.test.ts](C:/AiAppDeployments/AgentSmith/daemon/tests/mcp-client.test.ts:37): added coverage for stale-child reaping and safe lease release behavior.

Verification:
- `npm run build` in `daemon/`: passed. `dist` is rebuilt and current.
- `npm test`: could not complete in this sandbox. Vitest/Vite/esbuild are blocked here by Windows child-process spawning with `spawn EPERM`, so the full suite could not be executed end to end.
- Ran a direct compiled-JS smoke check with `node` against `dist` covering the new paths:
  - non-`agentsmith` self-attestation returns `eights-attest-invalid-consumer`
  - constitution cache refreshes after file mutation
  - singleton lease reaps a stale Eights child and clears the lease

