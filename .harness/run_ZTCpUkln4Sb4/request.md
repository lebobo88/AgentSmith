# Request

Harden AgentSmith's constitution attestation against the false eights-attest-hash-mismatch class of failure. THREE fixes in the AgentSmith repo: (1) daemon/src/bridges/eights-bridge.ts constitutionAttest(): the N8 self-attestation always compares TheEights' returned content_hash against AgentSmith's OWN local constitution hash, but the `consumer` parameter selects WHICH registered constitution TheEights returns — passing any consumer other than 'agentsmith' (e.g. consumer=hydra) makes TheEights return a different system's constitution, guaranteeing a false 'eights-attest-hash-mismatch'. Fix: pin consumer='agentsmith' internally for the local-hash comparison (or reject a non-agentsmith consumer on the N8 self-attest path) so AgentSmith can never compare its local hash against a peer's constitution; preserve N8 fail-closed semantics for a genuine mismatch of AgentSmith's own constitution. (2) daemon/src/inspector/invariants.ts getConstitution() (~lines 72-79): the module-level singleton cache ignores the `path` argument after first load and never auto-refreshes, so a long-running daemon can attest against a stale snapshot. Fix: key the cache by path AND invalidate on file mtime/size change (cheap statSync), preserving the 'loaded once per session' integrity intent. (3) Add a single-instance / orphan-reap guard so stale stdio daemon children from prior sessions cannot accumulate and contend on the shared ~/.eights/state.db. Rebuild daemon/dist, add/extend tests, keep the existing suite green. --repo agentsmith
